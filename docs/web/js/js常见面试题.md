# js 常见面试题

1. typeof 能判断哪些类型？

2. 何时使用`===`,何时使用`==`?

3. `window.onload` 和 `DOMContentLoaded` 的区别？

4. JS 创建 10 个 `a` 标签，点击时弹窗它的序号？

5. 手写节流 `throttle` 和防抖 `debounce`?

6. Promise 解决什么问题？

7. 手写深拷贝？
